import { type NavigationLink, NAVIGATION_LINKS } from 'src/shared/config';

export const headerTabLinks = [
  NAVIGATION_LINKS.CINEMA,
  NAVIGATION_LINKS.LIBRARY,
  NAVIGATION_LINKS.GAMES,
] satisfies readonly NavigationLink[];
